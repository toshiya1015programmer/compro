#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using namespace atcoder;

int main() {
  double a, b;
  cin >> a >> b;
  cout << fixed << setprecision(16) << (a - b) / 3 + b << endl;
  return 0;
}
